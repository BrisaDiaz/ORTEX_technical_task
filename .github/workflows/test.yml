name: Unit Tests
on: [push]
jobs:
  Jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Tests
        run: |
          npm ci 
          npm run lint
          npm test:ci
