name: Unit Tests
on: [push]
jobs:
  Jest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Unit Tests
      with:
- run: npm ci
- run: npm run lint
- run: npm test
